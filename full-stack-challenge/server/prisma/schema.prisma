
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model Token {
  id        String   @id @default(cuid())
  name      String
  imageURL  String
  address   String
  decimals  Int
  ticker    String
}